# Практическая работа 5-3-2

* Задача 1
    + создайте репозиторий
    сделано rdocker push roman5116/test:
/romanru5116_docker
    + выберите любой образ с NGINX

выбран сам NGINX  https://hub.docker.com/_/nginx
docker pull nginx

+ создайте форк 
docker pull nginx
 - тестовые папки для сборки nginx
mkdir -p ~/docker/nginx
mkdir -p ~/docker/nginx/html
 + реализуйте функциональность
 + редактируем index.html
nano ~/docker/nginx/html/index.html и туда текст
 + создаем Dockerfile
 nano ~/docker/nginx/Dockerfile
 + в описании конте1нера что статик html надо разместить в /usr/share/nginx/html"
 + переходим в директорию с Dockerfile (/home/rrm/docker/nginx)
 

+ даем docker build -t romanru5116 .
 _
- создается
- > [1/2] FROM docker.io/library/nginx                                    0.0s
 => CACHED [2/2] COPY html /usr/share/nginx/html  

 +] Building 0.2s (7/7) FINISHED   => =naming to docker.io/library/romanru5116                                docker:default
+ проверяем что есть 
 docker images
- почистим от изыбыточных образов с прошлых попыток
docker rmi
- отправляем в репозиторий:
- не отправляется
    rrm@smart:~/docker/nginx$ docker push roman5116/test:latest
The push refers to repository [docker.io/roman5116/test]
An image does not exist locally with the tag: roman5116/test

 исправлем:
 надо корректно тэгировать образ
 для этого смотрим какие есть контейнеры:
 - далее docker tag 1684f206de6f roman5116/romanru5116_docker
 - docker login
 - docker push roman5116/romanru5116_docker
+ результат в репозитории https://hub.docker.com/repository/docker/roman5116/romanru5116_docker/general

* Задание2

+ что использовать:Подходит ли в этом сценарии использование Docker-контейнеров или лучше подойдёт виртуальная машина, физическая машина? Может быть, возможны разные варианты?»
- высоконагруженное монолитное Java веб-приложение
+ пишут что формально нет WEB монолитов: архитектура (клиент_сервер)Но пусть  это монолит, и  тут сайзинг возможен вертикально до определенного предела. Если среда виртуализации - то с аппаратной поддержкой и на уровне ОС.
+ Nodejs веб-приложение;
пишут про неблокирующися ввод вывод - и работу с потокамипотоковой передачи данных - наверно это облегчает масштабирование и требует быть ближе к "физике"- VMWare
+ мобильное приложение c версиями для Android и iOS;
в контексте вопроса непонятно что отвечать
+ шина данных на базе Apache Kafka;
пишут про успешный опыт горизонтальное масштабирования
 интернет находил упоминания про успешный опыт горизонтальное масштабирования
+ Elasticsearch-кластер для реализации логирования продуктивного веб-приложения — три ноды elasticsearch, два logstash и две ноды kibana
вообще пишут про масштабирование и собраны контейнеры для докер
 + Elasticsearch пишут в Интернет про масштабирование и поднастройку,собраны контейнеры для докер
+ мониторинг-стек на базе Prometheus и Grafana;
Prometeus есть в неофицальных версиях для докер - по описанию это БД с метриками- надо смотреть по месту, насколько БД успевает это собирать
+ MongoDB как основное хранилище данных для Java-приложения;
это горизонтально масштабируемая БД,  по идее виртуализация как раз и нужна наращивать число узлов
+ Gitlab-сервер для реализации CI/CD-процессов и приватный (закрытый) Docker Registry.
сведения про GIT Enterprise: горизонтальное масштабирование для сред с десятками тысяч разработчиков, доступна топология кластера.

